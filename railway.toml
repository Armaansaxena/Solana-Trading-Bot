[build]
builder = "nixpacks"

[deploy]
startCommand = "npx tsx src/index.ts"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "always"

# block to hard-throttle resource usage
[services.bot.resources]
cpu = 0.1
memory = 128